{
  "name": "open-human-design",
  "version": "0.1.0",
  "description": "Open Human Design — an open-source, interactive Human Design chart app",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "natalengine": "file:../natal-engine"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
